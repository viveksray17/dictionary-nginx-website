<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width">
    <link rel="icon" type="image/x-icon" href="{{ url_for('static', filename='dictionary.png') }}">
    {% if title %}
      <title>{{ title }} - Dictionary</title>
    {% else %}
      <title>Dictionary Site</title>
    {% endif %}
  </head>
  <body>
    {% block content %}{% endblock content %}
  </body>
</html>
